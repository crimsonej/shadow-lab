# --- Core Dependencies (Zero Compilation Required) ---
flask>=2.0.0
requests>=2.25.1
httpx>=0.23.0
flask-cors>=3.0.10
paramiko>=3.0.0

# --- Optional FastAPI Engine ---
# By default, we use Flask to ensure zero build errors on diverse Python versions.
# If you want to use the high-performance async FastAPI engine, you can manually
# install the following dependencies if your system supports binary compilation:
# fastapi<=0.95.2
# uvicorn
